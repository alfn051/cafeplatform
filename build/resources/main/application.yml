spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dbseven
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    #    properties:
    #      hibernate:
    #        dialect: org.hibernate.dialect.MySQLDialect
    generate-ddl: true

  #      format-sql: true
  servlet:
    multipart:
      max-file-size: 5MB

  devtools:
    livereload:
      enabled: true
    thymeleaf:
      cache: false
      prefix: classpath:/templates/
    restart:
      enabled: true


server:
  servlet:
    session:
      tracking-modes: cookie

default_color: "#8a4500"
kakao_api_key: "c577b3ea3b76692b990e5674df1e0fb5"
distance_range: 1000
